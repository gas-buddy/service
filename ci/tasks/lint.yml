---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 8

inputs:
  - name: gasbuddy-service
  - name: dependency-cache

run:
  path: sh
  dir: gasbuddy-service
  args:
  - -exc
  - npm run lint